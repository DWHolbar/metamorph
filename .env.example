# Optional: GitHub Personal Access Token
# Without it, the GitHub API allows 60 requests/hour (unauthenticated).
# With it, you get 5,000 requests/hour — strongly recommended.
# Create one at: https://github.com/settings/tokens (no special scopes needed for public repos)
GITHUB_TOKEN=

